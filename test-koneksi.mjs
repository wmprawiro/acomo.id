import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function test() { console.log('Ping Supabase URL:', process.env.NEXT_PUBLIC_SUPABASE_URL); const { data, error } = await supabase.auth.getSession(); if(error) { console.error('Gagal:', error.message); } else { console.log('Koneksi Berhasil! Response Supabase:', data); } } test();  
