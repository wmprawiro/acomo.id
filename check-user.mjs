import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function check() { const { data } = await supabase.auth.admin.listUsers(); console.log(JSON.stringify(data.users[0], null, 2)); } check();  
